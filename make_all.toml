name = "SuperStacker"

love_files = [
"+*",
"-*/.*",
]

default_targets = ["win32", "win64", "lovejs", "macos"]

build_directory = "bin"

[lovejs]
title = "Super Stacker"  # used on the resulting web page
memory = "20000000"  # starting memory of the webpage (default is 20 MB)